# Email Builder - Versao Local

Esta pasta contem uma copia independente do Email Builder para uso offline.

## Como executar

```bash
chmod +x start.sh
./start.sh
```

O aplicativo sera disponibilizado em `http://localhost:3000`.
